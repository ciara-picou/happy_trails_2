<h1 class="header">Check out our trails: </h1>
    <% @trails.each do |trail| %>
        <li><%= link_to trail.name, trail_path(trail) %></li>
    
    <% end %>

    <%= button_to "Return to Homepage", user_path(session[:id]), method: "get" %>
